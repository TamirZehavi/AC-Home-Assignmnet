{
  "name": "ac-home-assignment",
  "version": "1.0.0",
  "engines": {
    "node": "^22.5.1",
    "npm": "^10.8.2"
  },
  "scripts": {
    "install:all": "npm install && cd shared && npm install && cd ../backend && npm install && cd ../frontend && npm install",
    "build:all": "cd shared && npm run build && cd ../backend && npm run build && cd ../frontend && npm run build",
    "start": "cd backend && npm run start"
  },
  "dependencies": {
    "@angular/cli": "^19.2.15",
    "@nestjs/cli": "^11.0.10"
  },
  "devDependencies": {
    "rimraf": "^5.0.10"
  }
}
